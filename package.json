{
  "name": "serverless-stripe",
  "version": "0.0.1",
  "engines": {
    "node": ">=14"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/aatos-app/serverless-stripe"
  },
  "description": "Serverless plugin for managing Stripe webhooks.",
  "author": "Aatos",
  "license": "MIT",
  "keywords": [
    "stripe",
    "serverless.com"
  ],
  "main": "dist/src/index.js",
  "bin": {},
  "scripts": {
    "lint": "tslint --project . && tslint --project tsconfig.json",
    "build": "tsc --project .",
    "prepare": "npm run build"
  },
  "files": [
    "*.js",
    "*.ts",
    "*.json",
    "dist/**/*.js"
  ],
  "devDependencies": {
    "serverless": "^3.38.0",
    "ts-node": "^10.9.1",
    "tslint": "^6.1.3",
    "typescript": "^5.2.2",
    "@serverless/typescript": "3.30.1"
  },
  "dependencies": {
    "stripe": "14.2.0"
  },
  "peerDependencies": {
    "serverless": "^3.0.0"
  }
}
